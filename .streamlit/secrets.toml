# Example Streamlit secrets (LOCAL TESTING only)
# Copy this file to `.streamlit/secrets.toml` and DO NOT commit it.

# Simple API key (preferred for listing public folder contents)
GDRIVE_API_KEY = "YOUR_GOOGLE_API_KEY_HERE"

# You can either paste the full service-account JSON (multiline) into
# `GOOGLE_SERVICE_ACCOUNT_JSON` or store it as a base64 string in
# `GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` (useful for the Streamlit Secrets UI).
# Example (raw JSON):
gcp_service_account = """
{
  "type" = "service_account"
"project_id" = "projeto-trafego-waze"
"private_key_id" = "SUA_KEY_ID"
"private_key" = "-----BEGIN PRIVATE KEY-----\nSUA_CHAVE_AQUI\n-----END PRIVATE KEY-----\n"
"client_email" = "trafego-foz-do-igua-u@projeto-trafego-waze.iam.gserviceaccount.com"
"client_id" = "103273267171906432789"
"auth_uri" = "https://accounts.google.com/o/oauth2/auth"
"token_uri" = "https://oauth2.googleapis.com/token"
"auth_provider_x509_cert_url" = "https://www.googleapis.com/oauth2/v1/certs"
"client_x509_cert_url" = "https://www.googleapis.com/robot/v1/metadata/x509/trafego-foz-do-igua-u%40projeto-trafego-waze.iam.gserviceaccount.com"}
"""

# Example (base64): generate base64 from your JSON and add the string here instead
# GOOGLE_SERVICE_ACCOUNT_JSON_BASE64 = "<LONG_BASE64_STRING>"


# Notes:
# - Do not commit your real credentials to git.
# - For Streamlit Cloud, add these keys in the app's Secrets UI instead of using this file.
